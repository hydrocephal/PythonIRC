[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "N", "RUF"]  # E=стиль, F=ошибки, I=сортировка импортов
ignore = ["E501", "B008", "S101"]

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"